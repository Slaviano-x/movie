buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath (libs.bundles.androidBundle)
        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.6.21'
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}